package dataStructures;

public class LinkCutTree {
	/*
	 * make-tree
	 * link two nodes
	 * cut - sever link betwee two trees
	 * find root - find the root given a vertex
	 * path-aggregate - given a vertex (find min, max, sum, etc.)
	 * 
	 * the core model is a splay tree
	 */
}
